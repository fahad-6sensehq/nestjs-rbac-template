// import { Utils } from './utils';

// export async function DeleteImageFromS3(imageUrl: string): Promise<void> {
//     if (imageUrl) {
//         const namePart = imageUrl.split('.s3.amazonaws.com/')[1];

//         const key = decodeURIComponent(namePart);

//         await Utils.deleteFile(key);
//     }
// }
